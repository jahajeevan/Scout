# Scout — backend dependencies (Python 3.11, macOS Apple Silicon).
# Install:  python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Gesture control (Phase 3, MediaPipe hand tracking) lives in a SEPARATE venv
# because MediaPipe requires numpy<2 and everything else here needs numpy>=2.
# See requirements-gesture.txt for that.

# ─── Web / async core ─────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
httpx==0.27.0
python-multipart==0.0.9

# ─── Voice pipeline ───────────────────────────────────────────────────
pywhispercpp==1.2.0             # local Whisper STT (whisper.cpp bindings)
kokoro-onnx==0.3.5              # local TTS (needs numpy>=2)
sounddevice==0.4.6
pyaudio==0.2.14
vosk==0.3.44                    # "Hey Scout" wake word (fully local)
pvporcupine==4.0.3              # optional alt wake engine (Picovoice)
openwakeword==0.6.0             # optional alt wake engine

# ─── Numerics ─────────────────────────────────────────────────────────
numpy>=2.0,<3                   # kokoro-onnx 0.3.5 works with numpy 2.x

# ─── Documents / RAG (SQLite-vector, no chromadb — see ARCHITECTURE) ──
pypdf==6.15.0
python-docx==1.2.0
Pillow>=11.0                    # image handling for /vision-chat + see_screen

# ─── System introspection ────────────────────────────────────────────
psutil==6.0.0

# ─── NL scheduling for routines/reminders ────────────────────────────
dateparser==1.2.0

# ─── Optional integrations ────────────────────────────────────────────
google-api-python-client==2.149.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
spotipy==2.24.0

# ─── Native macOS app (menu bar + Halo overlay + Spotlight panel) ─────
# Only needed if you're running the Scout.app native shell (macos/scout.py).
# Safe to install on other platforms too — they'll just no-op.
rumps==0.4.0
pyobjc-framework-Cocoa==12.2.2
pyobjc-framework-Quartz==12.2.2
pyobjc-framework-AVFoundation==12.2.2
pyobjc-framework-ApplicationServices==12.2.2
pyobjc-framework-WebKit==12.2.2
